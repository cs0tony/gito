{
  "name": "gito",
  "version": "0.0.3",
  "description": "Git safe merge tool - A CLI utility for safe git branch merging",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "gito": "./dist/cli.js"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build src/cli.ts --outdir ./dist --target bun",
    "prepublishOnly": "bun run build"
  },
  "private": false,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ]
}
